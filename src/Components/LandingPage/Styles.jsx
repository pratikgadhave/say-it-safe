import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles((theme) => ({
  carousel: {
    width: "100vw",
    Height: "200px",
  },
  Landing: {
    margin: "auto",
  },
}));
